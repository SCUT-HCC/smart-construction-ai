# 合规标准库

> **K18 产出** | 更新日期：2026-02-24

本目录存储施工方案编制依据相关的规范标准数据，供审核系统（`TimelinessChecker`）和生成系统（Ch1 Agent）使用。

---

## 文件说明

| 文件 | 格式 | 用途 |
|------|------|------|
| `standards_database.json` | JSON | **机器可读**的结构化标准数据库，供 `TimelinessChecker` 消费 |
| `reference_standards.md` | Markdown | **人类可读**的标准清单，按类别分表（K11 产出，保留不修改） |

---

## standards_database.json

### 数据概览

| 维度 | 数值 |
|------|------|
| 总条目 | 84 条 |
| 已校验 | 42 条（所有 ★★ 核心标准 + 大部分 ★ 高频标准） |
| 类别数 | 10 个 |
| 已知替代关系 | 8 组 |

### 字段定义

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `id` | string | ✅ | 唯一标识。有编号：`{前缀}_{编号}_{年份}`；法律法规：`LAW_{简称}`；规章：`REG_{简称}` |
| `standard_number` | string/null | ✅ | 完整标准编号（如 `GB 50300-2013`）；法律法规填 `null` |
| `standard_prefix` | string | ✅ | 标准类别前缀：`GB`/`GB/T`/`DL`/`DL/T`/`JGJ`/`Q/CSG`/`CECS`/`法规`/`部门规章` 等 |
| `number_body` | string/null | ✅ | 标准编号主体（不含前缀和年份），如 `50300`；法规填 `null` |
| `version_year` | int/null | ✅ | **当前有效版本**年份；无版本信息填 `null` |
| `title` | string | ✅ | 标准全称 |
| `type` | enum | ✅ | `国家标准`/`推荐性国标`/`行业标准`/`推荐性行标`/`企业标准`/`法律法规`/`部门规章`/`协会标准` |
| `status` | enum | ✅ | `现行`/`废止`/`已替代`/`待查` |
| `replaced_by` | string/null | — | 如已废止，填替代标准编号 |
| `replaces` | string/null | — | 本标准替代的旧标准编号 |
| `category` | string | ✅ | 归属类别（与 `reference_standards.md` 的 9 大分类 + 附录对齐） |
| `applicable_engineering_types` | string[] | ✅ | 适用工程类型（`变电土建`/`变电电气`/`线路塔基`/`设备安装`/`涂装工程`/`专题方案`/`绿色施工`/`通用`） |
| `applicable_chapters` | string[] | ✅ | 适用章节（`ch01`~`ch10`） |
| `citation_frequency` | string | ✅ | 引用频率：`★★`(核心必引) / `★`(高频) / `—`(低频) |
| `verified` | bool | ✅ | 是否已核实为当前最新版本 |
| `source` | string/null | — | 版本信息来源 |
| `notes` | string/null | — | 备注 |

### TimelinessChecker 集成说明

**查询逻辑**（伪代码）：

```python
import json, re

# 1. 加载数据库，建立 number_body 索引
with open("standards_database.json") as f:
    db = json.load(f)
index = {}
for s in db["standards"]:
    if s["number_body"]:
        index[s["number_body"]] = s

# 2. 从文档第一章提取引用的标准
pattern = r"(GB(?:/T)?|DL(?:/T)?|JGJ(?:/T)?|Q/CSG)\s*([\d.]+(?:-[\d.]+)?)-(\d{4})"
citations = re.findall(pattern, chapter1_text)

# 3. 逐条比对
for prefix, number, year in citations:
    match = index.get(number)
    if not match:
        continue  # 数据库中无此标准，跳过
    if match["status"] == "已替代":
        report.error(f"🔴 {prefix} {number}-{year} 已废止，替代为 {match['replaced_by']}")
    elif match["version_year"] and int(year) < match["version_year"]:
        report.warning(f"⚠️ {prefix} {number}-{year} 非最新版，当前版本为 {match['standard_number']}")
```

**关键约定**：
- `number_body` 为查询主键（同一标准不同版本共享此值）
- `version_year` 为当前有效版本年份，用于版本比较
- `standard_number` 为 `null` 的条目（法律法规、无编号企标）由 `TimelinessChecker` 跳过
- `status: "待查"` 的条目不触发告警

### 已知版本替代关系

| 当前版本 | 替代旧版 | 说明 |
|---------|---------|------|
| GB 50300-2013 | GB 50300-2001 | 建筑工程施工质量验收统一标准 |
| GB 50204-2015 | GB 50204-2002 | 混凝土结构工程施工质量验收 |
| GB 50202-2018 | GB 50202-2002 | 建筑地基基础工程施工质量验收 |
| GB 175-2020 | GB 175-2007 | 通用硅酸盐水泥 |
| GB 1499.2-2018 | GB 1499.2-2007 | 热轧带肋钢筋 |
| GB 50205-2020 | GB 50205-2001 | 钢结构工程施工质量验收 |
| GB/T 8923.1-2011 | GB 8923-88 | 涂装前钢材表面处理（标准重编号） |
| 住建部令第 37 号 | 建质〔2009〕87 号 | 危险性较大分部分项工程安全管理 |

---

## 维护指南

### 新增标准

1. 在 `standards` 数组末尾追加新条目
2. 确保 `id` 全局唯一
3. 更新顶层 `total_count`
4. 如已校验版本，同时更新 `verified_count`

### 更新标准版本

1. 修改 `version_year` 为新版本年份
2. 更新 `standard_number` 为新编号
3. 将旧编号填入 `replaces`
4. 设置 `verified: true`、`source` 为校验来源
5. 如旧版已有独立条目，将其 `status` 改为 `已替代`，`replaced_by` 指向新编号

### 废止标准

1. 将 `status` 改为 `已替代` 或 `废止`
2. 填写 `replaced_by`（如有替代标准）
3. 在 `notes` 中说明废止原因

### 校验建议

- 建议每半年校验一次核心标准（★★）的版本状态
- 校验来源优先使用国家标准全文公开系统 (openstd.samr.gov.cn)
- 企业标准（Q/CSG）需通过南网内部渠道确认

---

## 数据来源

| 来源 | 说明 |
|------|------|
| `reference_standards.md` | 基础数据（82 条，从 16 份施工方案中提取） |
| `ch01_编制依据.md` §4 | 工程类型→标准速查表，用于填充 `applicable_engineering_types` |
| `ch06_templates/` | 各模板验收标准章节，用于填充 `applicable_chapters` |
| 已知行业知识 | 8 组版本替代关系的标注 |
